# V4 SaaS 计划：VPS 上的托管订阅服务（含成本控制）

## Summary

V4 把当前单管理员、自用型工具升级为 **个人订阅 SaaS**，优先在 `VPS + Docker Compose` 上跑通，不先做云开发平台版本。产品方向已锁定为：

- 平台托管用户的笔记空间、FNS 和图床
- 用户通过邮箱验证码注册登录
- 注册后获得 **10 次免费剪藏体验**
- 后续通过 **国内聚合支付** 开通订阅
- 平台提供共享官方 `Telegram Bot + 飞书 Bot`
- 用户通过网页生成绑定码完成 Bot 绑定，不需要自己申请 Bot
- 数据库使用 `PostgreSQL`
- 成本策略默认 **严控可变成本**，尤其限制图床和全文 AI 润色

文档落地与分支准备也纳入计划：

- 把 V4 方案写入新的设计文档
- 基于当前 `v3` 新开 `v4` 分支承载后续实现

## Key Changes

### 1. 产品与账号模型

- 账号体系采用 **邮箱验证码登录/注册**，不做密码体系。
- 用户注册后默认获得 `10 次免费体验额度`。
- 订阅结构先做最小闭环：
  - `免费体验`
  - `基础付费`
- 当前管理员后台保留，新增普通用户控制台。
- 用户控制台至少提供：
  - 当前套餐与剩余额度
  - Bot 绑定码生成
  - 已绑定 Bot 账号查看
  - 托管空间信息查看

### 2. 数据与基础设施

- 用户级数据迁移到 `PostgreSQL`，新增核心表：
  - `users`
  - `user_workspaces`
  - `subscriptions`
  - `usage_counters`
  - `bot_bindings`
  - `payment_orders`
- 当前 `runtime-config.json` 只保留平台级配置：
  - 官方 Telegram / 飞书 Bot 凭据
  - 平台托管 FNS
  - 平台托管图床
  - 平台支付网关配置
- 每个用户默认一个个人 workspace，对应独立逻辑空间：
  - 独立 vault / namespace
  - 独立图床路径前缀
  - 独立转换历史与额度统计

### 3. 共享官方 Bot 与绑定替代白名单

- 平台统一提供一套官方 Telegram Bot 和一套官方飞书 Bot。
- 用户不再自己申请 Bot，也不再手工理解 `open_id/chat_id` 白名单。
- 绑定流程固定为：
  - 用户网页登录
  - 生成一次性短时效绑定码
  - 将绑定码发送给官方 Bot
  - 平台把该次消息来源的 `telegram_chat_id` 或 `feishu_open_id` 绑定到该用户
- SaaS 版“白名单”不再是手工配置字段，而是 `bot_bindings` 中已绑定身份集合。
- Bot 收到消息后按绑定结果路由到具体用户，再校验订阅与额度。

### 4. 托管 FNS、图床与 Obsidian 接入

- V4 默认平台托管 FNS 与图床，不做“用户自带 FNS/图床”。
- 用户在 Obsidian 中连接平台提供的托管 FNS 端点/个人空间。
- 所有转换输出写入用户自己的托管空间，不与其他用户混写。
- 图片托管默认也按用户空间前缀隔离。

### 5. 国内支付与订阅开通

- 支付方式固定为 **国内聚合支付层**，不直接分别实现微信支付和支付宝两套直连。
- 支付成功后自动更新：
  - `subscriptions`
  - `usage_counters`
  - 用户当前可用套餐状态
- 第一版不做复杂计费能力：
  - 不做优惠券
  - 不做企业采购流
  - 不做自动退款流程

### 6. 成本控制策略（V4 核心约束）

- 成本分级原则：
  - 固定成本：VPS、数据库、基础运维，可接受
  - 可变成本：图床流量、对象存储容量、AI 请求量，必须严控
- 免费体验用户默认能力：
  - 10 次单篇转换
  - 默认 `微信原链`
  - 默认不开全文润色
  - 不开放批量转换
- 基础付费用户默认能力：
  - 单篇转换
  - Bot 入口
  - 基础 AI 提炼（frontmatter / summary / tags）
  - 可选平台图床，但受月上传额度限制
  - 全文润色仅少量额度
- 成本风控必须进入 V4 基础设计：
  - 单用户月转换额度
  - 单用户 AI 润色额度
  - 单用户全文润色额度
  - 单用户图床上传总量额度
  - 单篇最大图片数
  - 单篇最大正文长度
  - Bot 触发限流与重复链接去重
- 超过成本边界时的默认降级策略：
  - 图床超额自动降级为 `微信原链`
  - 全文润色额度不足时只允许轻量 AI 提炼或完全禁用 AI
  - 超月额度直接拒绝转换并提示订阅/续费

## Public Interfaces / Behavior

- 新增用户侧页面/API：
  - 邮箱验证码登录/注册
  - 用户套餐与额度页
  - Bot 绑定码接口
  - 已绑定账号查询
  - 订单与支付状态查询
- 现有 Bot webhook 升级为：
  - 共享官方入口
  - 按 `bot_bindings` 路由到用户
  - 按订阅状态与额度判定是否执行
- 现有单租户 FNS/图床逻辑升级为：
  - 平台级服务配置
  - 用户级逻辑隔离
- 第一版不支持：
  - 用户自带 FNS
  - 用户自带图床
  - 用户自带 Bot
  - 匿名试用
  - 团队版/组织空间
  - 多付费档位

## Test Plan

- 账号与注册
  - 邮箱验证码注册、登录、过期、重复使用
- 免费体验
  - 新用户默认获得 10 次额度
  - 第 11 次未订阅时被拒绝
- Bot 绑定
  - 绑定码生成、过期、重复使用、误绑防护
  - Telegram / 飞书都能正确绑定到对应用户
- 多租户隔离
  - 两个用户通过共享 Bot 发链接，结果进入各自 workspace
  - 用户 A 不能读写用户 B 的数据与历史
- 订阅与支付
  - 支付成功后自动开通基础订阅
  - 过期或未订阅用户被拒绝
- 成本控制
  - 图床超额时自动降级为微信原链
  - 全文润色额度不足时被拒绝或降级
  - 单篇图片数和正文长度超限时拒绝
  - Bot 频率限制生效
- 回归
  - 当前单机模式仍可保留为平台后台/开发模式
  - 现有 Telegram / 飞书 webhook 基础链路不回退

## Assumptions

- V4 第一阶段只做 **个人托管 SaaS**。
- 数据库固定为 `PostgreSQL`。
- 部署固定为 `VPS + Docker Compose`，云开发平台后续再评估。
- 支付固定为 **国内聚合支付层**。
- 免费体验固定为 **注册后送 10 次**。
- 套餐结构固定为 **免费体验 + 基础付费**。
- 共享官方 Bot 固定为 `Telegram + 飞书` 双渠道。
- 实施前置动作包含：
  - 写出一份新的 V4 设计文档
  - 基于当前代码新建 `v4` 分支
